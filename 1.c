#include <stdio.h>

/**
 * @brief The entry point of the program.
 * * The main function prints the classic greeting message 
 * to the standard output (the console).
 * * @return 0 upon successful execution.
 */
int main() {
    // printf is a standard library function used to output formatted text.
    printf("Hello, C programming world!\n");
    
    // Return 0 to indicate successful execution to the operating system.
    return 0;
}